import toAssetVariable from '@/lib/template/toAssetVariable';

import variableName from '%/lib/liquid/variableName.mock';

const test = [1, 2, 3];

describe('lib.template.toAssetVariable', () => {
  it('should run tests', () => {
    expect(true).toBe(true);
  });
});
